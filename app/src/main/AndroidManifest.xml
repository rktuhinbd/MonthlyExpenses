<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rktuhinbd.smartmessmanager">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Dialog.AddRentDialog" />
        <activity
            android:name=".Activity.RentActivity"
            android:label="@string/title_activity_rent"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.MemberInformationActivity" />
        <activity android:name=".Dialog.AddMemberDialog" />
        <activity android:name=".Activity.BalanceSheetActivity" />
        <activity android:name=".Activity.MealRateActivity" />
        <activity android:name=".Activity.ExpenseActivity" />
        <activity android:name=".Activity.MealActivity" />
        <activity
            android:name=".Activity.MembersActivity"
            android:label="@string/title_activity_members"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>